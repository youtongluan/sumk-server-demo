<?xml version="1.0"?> 
<!DOCTYPE sdb SYSTEM "https://youtongluan.github.io/sumk/sql.dtd">
<sdb namespace="student">
<sql id="select">
	select id,name,age,last_update as lastUpdate from student where 1=1
	<if test="id">and id = #{id}</if>
	<if test="name" falseby="empty">and name= #{name}</if><!-- 空字符串会被过滤掉，不作为查询条件 -->
	<if test="age">and age= #{age}</if>
	<if test="lastUpdate">and last_update= #{lastUpdate}</if>
</sql>
<sql id="selectByIds">
	select id,name,age,last_update as lastUpdate from student 
	<if test="ids">
		where id in 
		<foreach collection="ids" item="id" separator="," open="(" close=")">#{id}</foreach>
	</if>
</sql>
</sdb>